import React, { useEffect, useMemo, useState } from 'react';
import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
  TextInput,
  FlatList,
  Image,
  StatusBar,
} from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';


const MovieHomeScreen: React.FC = () => {
  return (
    <View>
      <Text>Movie Home Screen</Text>
    </View>
  );
};

export default MovieHomeScreen;
